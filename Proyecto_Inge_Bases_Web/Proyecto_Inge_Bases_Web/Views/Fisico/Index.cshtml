@model IEnumerable<Proyecto_Inge_Bases_Web.Models.Fisico>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Producto.Nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Producto.Nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FisicoID }) |
            @Html.ActionLink("Details", "Details", new { id=item.FisicoID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FisicoID })
        </td>
    </tr>
}

</table>
